#!/usr/bin/env bash

cat << EOF
---
config_vars:
  PATH: "/app/bin:/usr/local/bin:/usr/bin:/bin"
default_process_types:
  web: /app/bin/logstash -f logstash.conf --enable-local-plugin-development
EOF
